{% extends 'base.html.twig' %}

{% block body %}
    <h1>Talla</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ talla.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ talla.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_gestiontalla_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_gestiontalla_edit', { 'id': talla.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
